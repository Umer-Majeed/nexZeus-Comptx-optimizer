<Window x:Class="NexZeus.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OverlayWindow"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="240" Height="160"
        Left="20" Top="20">
    <Border x:Name="RootBorder" Background="#AA0B0C0E" CornerRadius="10" Padding="12,10"
            MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
        <StackPanel>
            <TextBlock x:Name="GameNameText" Text="—" Foreground="#8A8F98" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock x:Name="FpsText" Text="FPS: --" Foreground="#A7D129" FontWeight="Bold" FontSize="16"/>
            <TextBlock x:Name="FrameTimeText" Text="Frame: -- ms" Foreground="#F9FAFB" FontSize="11" Margin="0,2,0,0"/>
            <TextBlock x:Name="PingText" Text="Ping: -- ms" Foreground="#F9FAFB" FontSize="11" Margin="0,2,0,0"/>
            <TextBlock x:Name="StutterText" Text="" Foreground="#FF4D4D" FontSize="10" FontWeight="Bold" Margin="0,2,0,0"/>

            <Border Background="#14161A" CornerRadius="6" Margin="0,8,0,0" Height="50" ClipToBounds="True">
                <Canvas x:Name="GraphCanvas" Width="216" Height="50">
                    <Polyline x:Name="FrameTimeGraph" Stroke="#A7D129" StrokeThickness="1.5"/>
                </Canvas>
            </Border>
        </StackPanel>
    </Border>
</Window>
